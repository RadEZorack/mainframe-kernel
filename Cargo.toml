[package]
name = "mainframe-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
# No std, no alloc yet
spin = "0.9"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"

[package.metadata.cargo-xbuild]
memcpy = true
