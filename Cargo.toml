[package]
name = "mainframe-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
spin = "0.9"

[build-dependencies]
cc = "1.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"